# Alpha 5 release checks

Date: 18 September 2026.

Candidate: `0.1.0a5`, based on `b855e64`, with the version, changelog, release
notes and tutorial/manual updates in this change. This is a preparation record.
No release has been published by these checks.

## Existing code checks

GitHub main was confirmed at `b855e64b6460e2aa03e726ae8c4b6f31e1438a6e`.
Its [CI run](https://github.com/zippergen-io/zippergen/actions/runs/35248192445)
passed all five jobs: tests on Python 3.11, 3.12 and 3.13, type checking, and
package building/installation. Those checks cover the local-model setup and
workflow skill changes being released.

This preparation changes no framework Python code. The final release commit
still needs its own green CI run before tagging.

## Documentation

- Rebuilt both committed PDFs and their source stamps with `make docs`.
- Checked the first tutorial prompt visually and through PDF text extraction.
  Its complete text is on page 3, with no leading `>` marker.
- Checked the manual's updated model setup and SSH tunnel section visually.
- Documentation, PDF source-stamp and skill-delivery checks: **18 passed**.
- `git diff --check` passed.

## Package checks

Built from a clean temporary source tree, excluding the untracked `spawn`
design. Build tools used an isolated environment. The wheel was built from
the source distribution.

- Wheel and source archive passed `twine check`.
- All **75** Python, Markdown and YAML files in the installed wheel payload
  match the release source, with no extra files of those types.
- A fresh core-only environment reports `0.1.0a5` and imports from the wheel.
  Ten CLI checks passed, including help, project initialization, bundled skill
  output, fully specified offline model setup, validation and full inspection
  of the email approval and work-pool examples.
- The installed work-pool example completed and resumed with the same result.
- The core-only environment reports the expected missing-extra error for
  Google support.
- A separate fresh environment installed the wheel with `[google]`. Google
  authentication and Gmail, Sheets, Calendar and Telegram helper imports passed.
  `uv pip check` found all 15 installed packages compatible.

The Google environment uses exactly the 14 dependency versions audited on
16 September, checked against that audit's JSON output. No new vulnerability
lookup was performed in this documentation/version change. The previous audit
and real Linux lifecycle results are in the
[alpha 4 record](2026-09-16-release-a4.md). The Linux lifecycle test was not
repeated here.

No live model call, OAuth authorization, message delivery or deployment change
was performed. The existing [security limits](../security.md) still apply.

## Local artifacts

Artifacts, temporary environments and smoke logs are under
`/private/tmp/zg-release-a5/`. These digests identify the local builds, not the
future GitHub publishing build.

| Artifact | SHA-256 |
| --- | --- |
| `zippergen-0.1.0a5-py3-none-any.whl` | `860004070d181ed2a8b4e14d6701d10bbb74cbadfa96af180ad4308c32768039` |
| `zippergen-0.1.0a5.tar.gz` | `d47b5ec99da3d79f82ede4bc48f99830d74e8bad5229f1d2c452fc47620305ec` |

Release notes: [0.1.0a5](../releases/0.1.0a5.md).

Next: commit and push the preparation, confirm CI for that commit, then create
the `v0.1.0a5` GitHub prerelease at that exact commit. Publishing the release
triggers the existing PyPI workflow.
